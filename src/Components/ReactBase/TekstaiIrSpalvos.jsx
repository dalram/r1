function TekstaiIrSpalvos ({firstText, secondText, textColor}) {
    return (
        <>
        <h1 style={{color: textColor}}>{firstText}</h1>
        <h2 style={{color: textColor}}>{secondText}</h2>
        </>
    )
}
export default TekstaiIrSpalvos;